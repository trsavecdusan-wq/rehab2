<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:label="rehab2"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true">
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <activity android:name=".AacCommunicatorActivity" />
        <activity android:name=".BackupSettingsActivity" />
        <activity android:name=".AacEditorActivity" />
        <activity android:name=".AacPackSettingsActivity" />
        <activity android:name=".PatientSetupWizardActivity" />
        <activity android:name=".LocalMusicActivity" />
        <activity android:name=".RadioStationEditActivity" />
        <activity android:name=".RadioPreviewActivity" />
        <activity android:name=".AddRadioStationActivity" />
        <activity android:name=".RadioSettingsActivity" />
        <activity android:name=".PowerSettingsActivity" />
        <activity android:name=".SettingsActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
